{{ template "header.tmpl" . }}
<h1>{{.PageTitle}}</h1>
<ul class="gamesList">
    {{range .Games}}
        <li class="game">
            <span class="gameTitle">{{.PrimaryTitle}}</span>
            <span class="gameYear">({{.Year}})</span>
            <span class="gameGeekRating">{{.BayesRating}}</span>
            <span class="gameAlgoRating">{{.AvgRating}}</span>
        </li>
    {{end}}
</ul>
{{ template "footer.tmpl" . }}